{
  "tasks": {
    "dev:k8s": "deno run --allow-read --allow-write --watch generate-k8s.ts",
    "dev:envs": "deno run --allow-read --allow-write --watch generate-envs.ts",
    "generate:k8s": "deno run --allow-read --allow-write  generate-k8s.ts",
    "generate:envs": "deno run --allow-read --allow-write generate-envs.ts"
  }
}
