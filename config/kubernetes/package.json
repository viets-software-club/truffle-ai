{
  "name": "deployment",
  "version": "1.0.0",
  "description": "Builds the kubernetes manifest files for the different deployments and services",
  "scripts": {
    "build": "OUT_DIR=../../.deployment sh ./build.sh"
  }
}
