{
  "name": "truffle-ai-backend",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "concurrently -c 'gray,magenta' 'npm run dev:graphql_gateway' 'npm run dev:graphql_server'",
    "dev:graphql_gateway": "npm run dev -w graphql_gateway",
    "dev:graphql_server": "npm run dev -w graphql_server",
    "build": "concurrently -c 'gray,magenta' 'npm run build:graphql_gatway' 'npm run build:graphql_server'",
    "build:graphql_gatway": "npm run build -w graphql_gateway",
    "build:graphql_server": "npm run build -w graphql_server",
    "serve": "concurrently -c 'gray,magenta' 'sleep 3 && npm run serve:graphql_gateway' 'npm run serve:graphql_server'",
    "serve:graphql_gateway": "npm run dev -w graphql_gateway",
    "serve:graphql_server": "npm run dev -w graphql_server",
    "test": "npm run format:check && npm run lint",
    "prepare": "if [ \"$NODE_ENV\" != \"production\" ]; then husky install; fi;",
    "lint": "eslint ./packages/**",
    "lint:debug": "eslint ./packages/** --debug",
    "lint:fix": "eslint ./packages/** --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "workspaces": [
    "packages/graphql_server",
    "packages/graphql_gateway"
  ],
  "lint-staged": {
    "*.{js,ts}": [
      "eslint ./packages/** --cache --fix",
      "prettier --write"
    ],
    "*.{html,css,scss,sass,md,mdx}": "prettier --write"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^18.16.3",
    "@types/showdown": "^2.0.1",
    "@typescript-eslint/eslint-plugin": "^5.59.2",
    "@typescript-eslint/parser": "^5.59.2",
    "concurrently": "^8.0.1",
    "eslint": "^8.39.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-import-resolver-typescript": "^3.5.5",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-sonarjs": "^0.19.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.2",
    "prettier": "^2.8.8"
  }
}
