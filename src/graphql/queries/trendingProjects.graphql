query TrendingProjects {
  projectCollection {
    edges {
      node {
        id
        name
        starCount
        issueCount
        forkCount
        pullRequestCount
        contributorCount
        githubUrl
        websiteUrl
        starHistory
        owningPerson
        owningOrganization
        associatedPerson {
          id
          login
          avatarUrl
        }
        organization {
          id
          login
          avatarUrl
        }
        isTrendingDaily
        isTrendingWeekly
        isTrendingMonthly
      }
    }
  }
}
