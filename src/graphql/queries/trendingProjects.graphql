query TrendingProjects {
  projectCollection {
    edges {
      node {
        id
        nodeId
        name
        starCount
        issueCount
        forkCount
        pullRequestCount
        contributorCount
        websiteUrl
        starHistory
        organization {
          id
          nodeId
          login
          avatarUrl
        }
      }
    }
  }
}
