query TrendingProjects {
  projectCollection {
    edges {
      node {
        id
        nodeId
        name
        starCount
        issueCount
        forkCount
        pullRequestCount
        contributorCount
        websiteUrl
        owned_by
        organization {
          id
          nodeId
          login
          avatarUrl
        }
      }
    }
  }
}
